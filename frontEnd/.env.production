# API base URL for production builds. The API is hosted separately, so set this
# to the deployed API's absolute URL (e.g. https://api.yoursite.com/api).
VITE_API_BASE_URL=https://api.example.com/api
